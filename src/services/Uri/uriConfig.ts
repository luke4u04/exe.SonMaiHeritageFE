export const baseUri = {
    base: 'http://localhost:8081/',
}

export const UriConfig = {
    Products: baseUri.base + 'api/products',
    Auth: baseUri.base + 'api/auth',
    Baskets: baseUri.base + 'api/baskets',
    Checkout: baseUri.base + 'api/checkout'
}

